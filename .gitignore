# ── macOS ───────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# ── Logs ─────────────────────────────────────────────────
*.log
app-debug.log
app-launchd.log
sidecar.log

# ── Python (sidecars) ────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
venv/
env/
.pytest_cache/

# Downloaded STT model weights (~2.5GB) — user data, never commit
sidecar/model/
sidecars/*/model/

# Sidecar runtime state — these are user data, not source
sidecar/config.json
sidecar/corrections.json
sidecars/*/corrections.json
# Warm-start cutover verdict — per-machine runtime state, written next to
# corrections.json (dev-source mode) or in Application Support (frozen install).
sidecar/warmstart.json
sidecars/*/warmstart.json

# Vendored ffmpeg — build artifacts produced by build-ffmpeg.sh / fetch-ffmpeg.ps1
# (macOS: built from source, LGPL; Windows: fetched LGPL static build). Not
# committed: large binaries, reproduced at build time.
sidecar/vendor/
sidecars/windows/vendor/

# PyInstaller build output (frozen sidecars)
sidecar/.venv-build/
sidecar/build/
sidecar/dist/
sidecars/windows/.venv-build/
sidecars/windows/build/
sidecars/windows/dist/

# ── Swift build artifacts ───────────────────────────────
SunoFlowApp/.build/
SunoFlowApp/SunoFlow.app
SunoFlowApp/signing/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# Throwaway prototypes compiled with swiftc (e.g. tools/answer-proto)
tools/*/.build/

# ── Release output (release.sh) ────────────────────────
*.dmg
SunoFlow.app.zip
# release.sh assembles the intermediate bundle at the repo root, not in SunoFlowApp/
/SunoFlow.app/

# ── .NET (Windows tray app) ────────────────────────────
apps/windows/SunoFlow/bin/
apps/windows/SunoFlow/obj/
apps/windows/SunoFlow/.vs/
# publish/ — output of `dotnet publish` (BUILD_WINDOWS.md §2 self-contained build)
apps/windows/SunoFlow/publish/
# windows.yml publishes from the repo root (-o publish\SunoFlow, no working-directory)
/publish/

# Windows installer staging + output (built by .github/workflows/release.yml)
apps/windows/installer/stage/
apps/windows/installer/output/

# ── Secrets / signing keys ──────────────────────────────
*.p12
*.cer
*.certSigningRequest
*.pem

# ── Editor / IDE ────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Local project memory (not for distribution) ─────────
.ogcode/
MEMORY.md
ogcode.json
# Firebase Functions dependencies
functions/node_modules/

# Local emulator secrets — never commit
functions/.secret.local

# Firebase CLI deploy cache — machine-local, regenerated on every deploy
.firebase/

# Cloudflare tunnel credentials — secret, never commit
cleanup-gateway/deploy/cloudflared/*.json

# Dictation smoke test droppings — the downloaded model and the run's lease.
# The WAV fixture it transcribes IS committed (sidecars/testdata/).
model-cache/
dictation-smoke-lease.json

# Firebase Functions — email templates are compiled by npm run build (source
# of truth is functions/emails/*.jsx); .env holds function params (RESEND_FROM)
functions/emails/*.js
functions/.env
functions/.env.*
